<UserControl x:Class="LIBBRARY_MANAGER.UI.Modules.EventCatalog.EventMainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:items="clr-namespace:LIBBRARY_MANAGER.UI.Modules.EventCatalog.Items" xmlns:localItems="clr-namespace:LIBBRARY_MANAGER.UI.Common.Items.MainView"
             Background="White">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <localItems:Header  
    Grid.Row="0"
    Background="White"
    HorizontalContentAlignment="Center"
    LogoPath="/UI/Modules/EventCatalog/Assets/Images/Noctua_Blue.png"
    SideImagePath="/UI/Modules/EventCatalog/Assets/Images/Noctua_On_Book.png"
    Title="Gestionnaire de Bibliothèque"
    Subtitle="Liste des événements"/>

        <!-- Separator -->
        <Rectangle Grid.Row="1" Height="2" Fill="#2A7FFF" Margin="0,5,0,5"/>

        <!-- Table Header -->
        <Grid Grid.Row="2" Background="#F0F4FF" Height="40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Title" FontWeight="Bold" FontSize="14" Foreground="#1E60FF" VerticalAlignment="Center" Margin="5,0"/>
            <TextBlock Grid.Column="1" Text="Start Date" FontWeight="Bold" FontSize="14" Foreground="#333" VerticalAlignment="Center" Margin="5,0"/>
            <TextBlock Grid.Column="2" Text="End Date" FontWeight="Bold" FontSize="14" Foreground="#333" VerticalAlignment="Center" Margin="5,0"/>
            <TextBlock Grid.Column="3" Text="Description" FontWeight="Bold" FontSize="14" Foreground="#2A7FFF" VerticalAlignment="Center" Margin="5,0"/>
        </Grid>

        <!-- Event List -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Events}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <items:EventListItem />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>